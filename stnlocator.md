# WIS2観測データのメタデータ欠落地点に対するオンライン位置推定

## 問題意識

WIS2で流通する地上気象観測の中には、経緯度や標高などのメタデータを欠くものが少なくない。これらは通常の利用が困難であるが、観測値そのものと数値解析値との比較により、おおよその位置を推定できる可能性がある。

目的は、

- 未知地点の緯度経度を推定する
- 推定精度を定量化する
- リアルタイム運用可能な計算量に抑える

ことである。

## 基本的な考え方

利用する解析値は GSM 解析（1.25度格子）とする。

未知地点について、

- 最新気圧値
- 6時間前気圧値
- 気圧の指数移動平均（EMA）

のみを保持する。

解析格子についても同様に、

- 最新値
- 6時間前値
- EMA

を保持する。

これにより長期間の時系列を保存せずに比較が可能となる。

## 特徴量

### 気圧偏差

気圧の絶対値は標高依存性が大きいため、EMAからの偏差を利用する。

\[
p' = p - \bar p
\]

ここで

- \(p\)：最新気圧
- \(\bar p\)：3～5日程度のEMA

である。

### 6時間変化量

\[
\Delta p = p(t)-p(t-6h)
\]

を計算する。

これにより、

- 低気圧通過
- 前線通過

などの時系列的特徴を抽出できる。

## 推定アルゴリズム

### 初期位置

メタデータから得られる情報

- 発信国
- WSI
- 地点番号

などを用い、最寄りの既知観測所を初期推定位置 \(P\) とする。

### 最適候補探索

現在の推定位置周辺（例：半径500 km）の格子点について、

\[
J =
w_1(p'_{obs}-p'_{gsm})^2
+
w_2(\Delta p_{obs}-\Delta p_{gsm})^2
\]

を計算する。

ここで \(J\) が最小となる格子点を \(Q\) とする。

必要に応じて気温偏差も加える。

\[
J =
w_pJ_p
+
w_{\Delta p}J_{\Delta p}
+
w_TJ_T
\]

### 位置更新

推定位置を直接 \(Q\) に移動させるのではなく、

\[
P' = kP + (1-k)Q
\]

で更新する。

ここで

\[
0 < k < 1
\]

である。

この緩和により振動や発散を抑制できる。

## 収束判定

位置更新量

\[
D = |P'-P|
\]

を計算する。

- \(D\) が徐々に減少 → 良好な収束
- \(D\) が大きく変動 → 位置不確定

と解釈する。

例：

位置推定:
15.3N, 102.1E
安定度:
0.92
現在の位置更新量:
12 km

## 利点

- 長期履歴を保持する必要がない
- リアルタイム処理に適する
- 標高不明でも利用可能
- WIS2上のメタデータ欠落観測を活用できる
- 推定位置だけでなく品質指標も提供できる

## 今後の検討課題

- 気温偏差の追加効果
- 湿度の利用可能性
- 最適なEMA時定数
- 最適な緩和係数 \(k\)
- 収束速度と推定誤差の関係
